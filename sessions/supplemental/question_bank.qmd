---
title: "Question Bank"
subtitle: "Every question in the workshop, organized by session and part"
author: "Immanuel Williams"
format:
  html:
    toc: true
    toc-depth: 2
    theme: cosmo
    css: ../../custom.css
---

All questions across Sessions 1–4, listed by session and part (each session has exactly four parts). **Types:** MC = multiple choice (five options, a–e), FIB = fill in the blank (one–two words), SA = short answer, ORD = ordering, DISC = discussion. The *Answer* column gives the correct letter for MC/ORD and the expected word(s) for FIB.

## Session 1 — API Mechanics & Response Fundamentals

| Part | # | Type | Question (abridged) | Answer |
|------|---|------|----------------------|--------|
| 1: What is an API? + Data Flow | Q1 | FIB | In the restaurant analogy, the waiter plays the role of the ____ | API |
| 1: What is an API? + Data Flow | Q2 | MC | Primary utility of a web API for a data analyst | d |
| 1: What is an API? + Data Flow | Visual Check | MC | The API's role between Client and Server | c |
| 1: What is an API? + Data Flow | Q3 | FIB | Client → ____ → Server → Database | API |
| 1: What is an API? + Data Flow | Q4 | FIB | Restaurant story, five blanks | client, waiter, kitchen, fridge, response |
| 1: What is an API? + Data Flow | Q5 | MC | API documentation is analogous to the ____ | e (the menu) |
| 2: What is JSON? (short tab) | Quick Check | SA | Point to a key, string, number, boolean, array, nested object; value of `name` | "Maya", inside the nested `customer` object |
| 2: What is JSON? (full tab) | Guided Activity | SA ×5 | Read the avocado-toast JSON: count top-level pairs; type of `vegetarian`; which key is an array; which is a nested object; value of `country` | 6; boolean `true`; `toppings` (2 items); `origin`; "Australia" |
| 2: What is JSON? (full tab) | Your Turn | Edit | Complete the coffee-order JSON (fill three typed blanks, add `"to_go": true`) | any values with correct types + comma before new pair |
| 2: What is JSON? | Q6 | MC | JSON's fundamental structure | c (key-value pairs) |
| 2: What is JSON? | Q7 | FIB | In R, a JSON object becomes a named ____ | list |
| 2: What is JSON? | Q8 | FIB | Flattened column name for latitude | coordinates.lat |
| 3: Requests, Responses & the JSON Body | Q9 | FIB | A request is written as a ____ | URL |
| 3: Requests, Responses & the JSON Body | Q10 | MC | The three things a response contains | a (data, metadata, status code) |
| 3: Requests, Responses & the JSON Body | Quick Checks | FIB ×3 | Body wrapped in curly ____; data, ____, status code; the ____ ____ tells if it worked | braces; metadata; status code |
| 4: Status Codes | Q11 | FIB | 4xx = ____ side; 5xx = ____ side | client; server |
| 4: Status Codes | Q12 | MC | Most likely source of a 404 | e (URL incorrect) |
| 4: Status Codes | Q13 | MC | What a 200 + JSON body contains | b |

## Session 2 — The Anatomy of a Request

| Part | # | Type | Question (abridged) | Answer |
|------|---|------|----------------------|--------|
| 1: The CRUD Framework | Visual Check | MC | Editing a display name maps to which operation | d (Update — PUT/PATCH) |
| 1: The CRUD Framework | Q1 | FIB | HTTP method used to read data | GET |
| 1: The CRUD Framework | Q2 | MC | Most common CRUD operation in data science | a (Read) |
| 2: GET & POST Requests | Visual Check | FIB | Changing `/users/42` to `/users/7` changes only the ____ | path |
| 2: GET & POST Requests | Q3 | MC | Which task is a GET request | e (retrieve NYT articles) |
| 2: GET & POST (optional POST tab) | Visual Check | MC | Where the POST payload lives | b (request body) |
| 2: GET & POST (optional POST tab) | Q4 | MC | Which task requires POST | c (submitting a tweet) |
| 2: GET & POST (optional POST tab) | Q5 | MC | POST in the restaurant analogy | a (submitting a new recipe) |
| 3: URL Anatomy | Visual Check | FIB ×2 | Boundary character; pair separator | `?`; `&` |
| 3: URL Anatomy | Q6 | SA | Identify endpoint + key-value pairs in Spotify URL | endpoint `/v1/search`; `q=Daft+Punk`, `type=artist` |
| 4: Putting It Together | Q7 | MC | GET sends a ____ and receives ____ | d (URL → JSON) |
| 4: Putting It Together | Q8 | ORD | Order the 5 request steps (moved here from Session 3) | b → e → d → c → a |
| 4: Putting It Together | Activity | Paper | Write your hometown Open-Meteo URL from the five-blank template | `https://api.open-meteo.com/v1/forecast?latitude=…&longitude=…&current=temperature_2m&temperature_unit=fahrenheit` |
| Take-Home Exercises page | Bonus Q | Open | GET vs. POST and JSON direction | JSON both ways in POST |

## Session 3 — Weather API — First Real Requests

| Part | # | Type | Question (abridged) | Answer |
|------|---|------|----------------------|--------|
| 1: Meet Open-Meteo + Browser Demo | Q1 | FIB | The stable part of the URL is the ____ | endpoint |
| 1: Meet Open-Meteo + Browser Demo | Q2 | MC | True statement about Open-Meteo | c (free, no key) |
| 1: Meet Open-Meteo + Browser Demo | Q3 | FIB | What the browser shows is raw ____ | JSON |
| 1: Meet Open-Meteo + Browser Demo | Q4 | MC | Address bar performs which HTTP request | e (GET) |
| 2: ETV Cycle + Extract in R | Q5 | FIB | The status number we hope to see | 200 |
| 2: ETV Cycle + Extract in R | Q6 | MC | What `req_url_query()` does | d |
| 3: Transform + Scaling with `map()` | Q7 | MC | Meaning of `\(city, latitude, longitude)` | b (anonymous function) |
| 3: Transform + Scaling with `map()` | Q8 | FIB | Stack one-row tibbles with ____ | bind_rows() |
| 4: Geocoding + Your Turn | Q9 | FIB | Geocoding converts a name into ____ and ____ | latitude; longitude |
| 4: Geocoding + Your Turn | Activity | Fill-in | Geocode your hometown, request current weather, build `my_df` | see Session 3 solution callout |

## Session 4 — Historical Weather, Visualization & Wrap-Up

| Part | # | Type | Question (abridged) | Answer |
|------|---|------|----------------------|--------|
| 1: Historical Weather — Full ETV Pipeline | Q1 | FIB | Parameters that set the date range | start_date; end_date |
| 1: Historical Weather — Full ETV Pipeline | Q2 | MC | What `unlist()` does | a (list → vector) |
| 2: Visualize — The V in ETV | Q3 | FIB | `ggplot()` is the tool for the ____ step | Visualize (V) |
| 3: Your Turn — Full Pipeline | Q4 (Summer) | MC | What distinguishes the two cities' lines | e (`aes(color = city)`) |
| 3: Your Turn — Full Pipeline | Q5 (Winter) | MC | What changed between summer/winter code | c (only the two dates) |
| 3: Your Turn — Full Pipeline | Q6 | FIB | Which city is much colder in January | Boston |
| 3: Your Turn — Full Pipeline | Activity | Fill-in | Pick 2–3 cities, run the pre-built pipeline, plot your variable | see Session 4 example solution |
| 4: Reflection & Survey | Q7 | DISC | Where could API data integrate into your curriculum | open discussion |

## Design Standards

- FIB (one–two words) is the primary type; MC always has five options (a–e) with obvious distractors
- Correct MC letters are deliberately spread: S1 {d, c, e, c, a, e, b}, S2 {d, a, e, b, c, a, d}, S3 {c, e, d, b}, S4 {a, e, c}
- No question uses a function before it has been introduced (Session 2's ordering question flags its R function names as a preview)
- Open-ended questions are limited; discussion prompts are reserved for teaching reflection
